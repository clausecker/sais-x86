Each item is prefixed with one of the following letters:

 B bugfix
 C compatible expansion without adding new symbols
 I incompatible change
 N new function, method or variable in the API
 R general remark

Release 0.1 (2019-01-19)
 R initial release
